# 02805---Social-graphs-RAM

The GitHub Pages workflow deploys the site on every push to `main`.
The repository's Pages source must be set to **GitHub Actions** by an administrator.

After the **Deploy site to GitHub Pages** workflow succeeds in the Actions tab,
view the site at https://monicahardt.github.io/02805-SocialGraphs-RAM/.
